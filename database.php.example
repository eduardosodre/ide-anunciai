<?php
/**
 * Database config — copy to database.php and fill with your values.
 * database.php is in .gitignore and must never be committed.
 */
return [
    'host'     => getenv('DB_HOST') ?: 'localhost',
    'database' => getenv('DB_NAME') ?: '',
    'user'     => getenv('DB_USER') ?: '',
    'password' => getenv('DB_PASS') ?: '',
    'charset'  => 'utf8mb4',
];
